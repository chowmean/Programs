#include<iostream>
#include<string>
using namespace std;
string sum(string a,string b)
{int alen=a.length()-1;
int blen=b.length()-1;
string result;
int c=0,remain==0;
while(alen>0 && blen>0)
{c=a[alen]-'0' + b[blen]-'0'+reamin;
if(c>10)
{remain=c/10;
c=c%10;
result.append(1,(char)c);
        }
 }      }

int main()
{int t;
cin>>t;string a,b,temp,final;
while(t--)
{cin>>a>>b;
int lena=a.length()-1;
int lenb=b.length()-1;

          }
    }
